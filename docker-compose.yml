version: '3.9'

services:
  db:
    image: postgres:16
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: welive
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
